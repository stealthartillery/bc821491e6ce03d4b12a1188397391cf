LGen.stat = {
	data : "Data",
	stat : "Stat",
	success : "success",
	failed : "failed",
	not_found : "not_found"
}

// (function() {
// 	LGen.obj.read(BASE_URL+'assets/gen.obj/globvar.lgen', function(obj) {
// 		var keys = Object.keys(obj);
// 		for (var i=0; i<keys.length; i++) {
// 			LGen.stat[keys[i]] = obj[keys[i]]
// 		}
// 	})
// })()