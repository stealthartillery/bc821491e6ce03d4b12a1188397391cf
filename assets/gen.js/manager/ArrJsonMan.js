LGen.ArrJson = new ArrJsonMan()
LGen.ArrJsonMan = new ArrJsonMan()

function ArrJsonMan () {

	// function is_key_exist (arrjson, str) {
	// 	for (var i=0; i<arrjson.length; i++) {
	// 		if (arrjson[i]['word'].toLowerCase() == str.toLowerCase()) {
	// 			return true;
	// 		}
	// 		else if (i==arrjson.length-1) {
	// 			return false;
	// 		}
	// 	}
	// }

}